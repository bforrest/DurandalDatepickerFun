define(['plugins/http', 'durandal/app', 'knockout'], function (http, app, ko) {
	 return {
        displayName: 'Using observable plugin, plus a value binder "hack" to pick a date',
        immaDate: '',
        activate: function () {
        }
    };
});